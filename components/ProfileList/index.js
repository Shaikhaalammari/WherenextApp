import React from "react";
import { observer } from "mobx-react";

// don't create this file if youre not currently working on the profile features
// keep the master branch clean all the time.

const ProfileList = () => {};

export default observer(ProfileList);
